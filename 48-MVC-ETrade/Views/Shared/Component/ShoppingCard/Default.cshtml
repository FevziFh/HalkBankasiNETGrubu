@using _48_MVC_ETrade.Models.VMs;
@model ShoppingCardVM

<div>
    <h3>Alıveriş Sepetim</h3>
    <ul>
        @foreach (var item in Model.Products)
        {
            <li>@item.Name - @item.Price</li>
        }
        <p>Toplam: @Model.Price</p>
    </ul>
</div>
